{
  "name": "claude-usage-proxy",
  "version": "1.0.1",
  "description": "Tiny proxy that fetches Claude.ai session/weekly usage server-side and serves it to the R1 Creation (avoids CORS + keeps your session key off the device).",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "node-fetch": "^2.7.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18"
  }
}
