{
  "name": "claude-usage-relay",
  "version": "2.0.0",
  "description": "Small relay: receives Claude.ai usage data pushed by a Chrome extension (running inside your authenticated browser session) and serves it to the R1 Creation. Does not talk to claude.ai itself.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18"
  }
}
