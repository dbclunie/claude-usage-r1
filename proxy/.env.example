# Copy to .env for local dev (never commit the real .env)
# CLAUDE_SESSION_KEY is no longer used as of relay v2.0.0 — the relay
# never talks to claude.ai directly. See extension/ for how usage data
# actually gets in.
PROXY_SHARED_SECRET=make-up-a-random-string
PORT=3000
