services:
  - type: web
    name: claude-usage-proxy
    runtime: node
    plan: free
    rootDir: proxy
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PROXY_SHARED_SECRET
        sync: false
      - key: PORT
        value: 10000
